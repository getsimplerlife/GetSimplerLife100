import { describe, expect, it } from "vitest";
import { salesCapabilities, readOpportunities, updateOpportunity } from "../agents/capabilities/sales";
describe("Sales / Salesforce capability slice", () => {
  it("keeps contracts unverified", () => expect(salesCapabilities.every((c) => c.status === "unverified")).toBe(true));
  it("fails closed without tenant or auth", async () => { const adapter = { listOpportunities: async () => [] } as any; await expect(readOpportunities(adapter, { tenantId: "", authToken: "x", audit: () => {} })).rejects.toThrow("Tenant scope"); await expect(readOpportunities(adapter, { tenantId: "t", audit: () => {} })).rejects.toThrow("authentication"); });
  it("retries bounded reads and audits", async () => { let calls = 0; const outcomes: string[] = []; const result = await readOpportunities({ listOpportunities: async (tenantId) => { calls++; expect(tenantId).toBe("t"); if (calls < 2) throw Error("temporary"); return ["opportunity"]; } }, { tenantId: "t", authToken: "token", maxAttempts: 2, audit: (event) => outcomes.push(event.outcome) }); expect(result).toEqual(["opportunity"]); expect(calls).toBe(2); expect(outcomes).toEqual(["succeeded"]); });
  it("requires idempotency and audits failed writes", async () => { const outcomes: string[] = []; const adapter = { updateOpportunity: async () => { throw Error("unavailable"); } }; await expect(updateOpportunity(adapter, {}, { tenantId: "t", authToken: "token", audit: (event) => outcomes.push(event.outcome) }, "")).rejects.toThrow("Idempotency"); await expect(updateOpportunity(adapter, {}, { tenantId: "t", authToken: "token", maxAttempts: 2, audit: (event) => outcomes.push(event.outcome) }, "k")).rejects.toThrow("unavailable"); expect(outcomes).toEqual(["failed"]); });
});
